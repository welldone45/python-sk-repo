'''
there are 3 ways remove elements form the list

remove() : remove elements using value
    e.g. list.remove(value)
pop()   : by default it will remove last index value
    e.g. list.pop()
         pop(index) : it will remove specific index wise value
         e.g. list.pop(index)
    clear() : remove all elements from the list
-------------------------------------------------------

del : del is a keyword which is used to delet list from the memory

'''